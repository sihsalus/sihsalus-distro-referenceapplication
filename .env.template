# SIH Salus - variables de entorno
#
# Desarrollo:
#   cp .env.template .env
#   docker compose up -d
#
# Producción: reemplaza todos los valores de ejemplo y fija tags de imagen.
# También puedes generar un archivo seguro con:
#   ./scripts/security/secrets_generate.sh

DEPLOYMENT_ENV=development

# Selección persistente del stack (recomendada en servidores)
# Evita omitir accidentalmente un override en comandos posteriores.
# HTTPS:
# COMPOSE_FILE=docker-compose.yml:compose/ssl.yml
# COMPOSE_PROFILES=ssl
# Keycloak + HTTPS:
# COMPOSE_FILE=docker-compose.yml:compose/keycloak.yml:compose/ssl.yml
# COMPOSE_PROFILES=keycloak,ssl
# Imaging autenticado + Keycloak + HTTPS:
# COMPOSE_FILE=docker-compose.yml:compose/keycloak.yml:compose/imaging-auth.yml:compose/ssl.yml
# COMPOSE_PROFILES=keycloak,imaging,ssl
# Imaging autenticado + OpenMRS con login local (Compose >= 2.24.4):
# COMPOSE_FILE=docker-compose.yml:compose/keycloak.yml:compose/imaging-auth.yml:compose/ssl.yml:compose/openmrs-local-auth.yml
# COMPOSE_PROFILES=keycloak,imaging,ssl

# Core
OPENMRS_DB_USER=openmrs
MYSQL_OPENMRS_PASSWORD=openmrs
MYSQL_ROOT_PASSWORD=openmrs
# Token API OCL crudo: 40 caracteres hexadecimales en minúscula, sin el prefijo "Token".
# Vacío no sobrescribe un token que OpenMRS ya tenga almacenado.
OMRS_OCL_TOKEN=
# Filtro backend local/basic. Fija false y recrea backend para desactivarlo.
SIHSALUS_FORCED_PASSWORD_CHANGE_ENABLED=true

# Imágenes. En producción usa referencias inmutables (por ejemplo sha-...@sha256:...).
BACKEND_TAG=latest
FRONTEND_SOURCE_TAG=sha-6cadce5b8a225bc6fe0715b9ff085ced5f9feacf@sha256:ada19c77f8d27ea50e89db4fdae7287bbdf3a51c9fab8ac95dd82eba36d9a28c
FRONTEND_RUNTIME_TAG=latest
# Imagen estática del portal de ayuda. En producción use el digest publicado.
DOCS_IMAGE_REF=ghcr.io/sihsalus/sihsalus-docs:latest
# FRONTEND_RUNTIME_IMAGE=sihsalus-frontend-runtime
STRIP_SOURCE_MAPS=true

# Réplica y backup MariaDB
# OMRS_DB_REPL_USER=openmrs_repl
# OMRS_DB_REPL_PASSWORD=
# OMRS_DB_BACKUP_USER=openmrs_backup
# OMRS_DB_BACKUP_PASSWORD=

# Gateway
# FRAME_ANCESTORS=
# ACL adicional a la autenticación individual de Imaging.
# IMAGING_NETWORK_ACCESS_CONTROL=allow 127.0.0.1; allow 10.0.0.0/8; allow 172.16.0.0/12; allow 192.168.0.0/16; deny all;

# FUA: docker compose --profile fua up -d
# FUA_GENERATOR_IMAGE=ghcr.io/sihsalus/generador-de-fua
# FUA_GENERATOR_TAG=sha-8deb5ba
# SIHSALUS_FUA_GEN_DB_USER=fuagenerator
# SIHSALUS_FUA_GEN_DB_PASSWORD=
# SIHSALUS_FUA_GEN_DB=fuagenerator
# SIHSALUS_FUA_GEN_TOKEN=
# SIHSALUS_FUA_GEN_SECRET_KEY=
# Compatibilidad por defecto; use un secreto de 12 bytes y no lo rote sin migrar los FUAs.
# SIHSALUS_FUA_GEN_ENCRYPTION_KEY=123456789012

# Indicadores: docker compose --profile indicadores up -d
# REPORTES_SQL_IMAGE=ghcr.io/sihsalus/reportes-sql
# REPORTES_SQL_TAG=latest
# SIHSALUS_REPORTES_SQL_DB=reportes_sql
# SIHSALUS_REPORTES_SQL_DB_USER=reportes_sql
# SIHSALUS_REPORTES_SQL_DB_PASSWORD=

# HAPI FHIR: docker compose --profile hapi up -d
# HAPI_IMAGE=johanamador/hapi-fhir-minsa-peru
# HAPI_TAG=latest
# HAPI_PORT=8085
# HAPI_DB_USER=hapi
# HAPI_DB_PASSWORD=
# HAPI_DB_NAME=hapi

# Keycloak (requiere cargar compose/keycloak.yml)
# docker compose -f docker-compose.yml -f compose/keycloak.yml --profile keycloak up -d
# KEYCLOAK_ADMIN=admin
# KEYCLOAK_ADMIN_PASSWORD=
# KC_DB_DATABASE=keycloak
# KC_DB_USERNAME=keycloak
# KC_DB_PASSWORD=
# KEYCLOAK_MODE=development
# KC_HOSTNAME=http://localhost/keycloak
# KEYCLOAK_PORT=8180
# KEYCLOAK_PUBLIC_URL=http://localhost/keycloak
# OPENMRS_REDIRECT_URI=http://localhost/openmrs/*
# OAUTH2_CLIENT_SECRET=
# IMAGING_OIDC_CLIENT_SECRET=
# IMAGING_OAUTH_REDIRECT_URI=http://localhost/imaging/oauth2/callback
# OAUTH2_ENABLED no es configurable: core=false y el override Keycloak=true.
# El override opcional compose/openmrs-local-auth.yml restaura false para OpenMRS.

# Monitoring: docker compose --profile monitoring up -d
# GRAFANA_ADMIN_USER=admin
# GRAFANA_ADMIN_PASSWORD=
# GRAFANA_ROOT_URL=http://localhost:3001
# Gateway access is CLOSED unless this Nginx geo allowlist is configured.
# Each entry is an authorized client CIDR followed by 1; (not allow/deny syntax).
# GRAFANA_NETWORK_ALLOWLIST=192.168.0.0/24 1;
# For HTTPS gateway access, set both values explicitly:
# GRAFANA_ROOT_URL=https://sihsalus.example.test/grafana/
# GRAFANA_COOKIE_SECURE=true
# ALERTMANAGER_EXTERNAL_URL=http://localhost:9093
# SIHSALUS_BACKUP_METRICS_PATH=/srv/sihsalus-backups
# SIHSALUS_SAMBA_QUOTA_BYTES=21474836480
# VIEWPOWER_BASE_URL=http://host.docker.internal:15178/ViewPower
# VIEWPOWER_DEVICE=
# VIEWPOWER_TIMEOUT_SECONDS=5
# VIEWPOWER_CACHE_SECONDS=10
# Grafana OIDC opt-in: docs/operations/grafana-oidc.md (existing Keycloak required).
# GRAFANA_OIDC_CLIENT_SECRET=
# OIDC requires GRAFANA_ROOT_URL=https://<host>/grafana/ and KEYCLOAK_PUBLIC_URL=https://<host>/keycloak

# Imaging: docker compose --profile imaging up -d
# ORTHANC_AUTHENTICATION_ENABLED=false
# DICOM_BIND_ADDRESS=127.0.0.1
# DICOM_PORT=4242
# ORTHANC_DICOM_CHECK_CALLED_AET=true
# ORTHANC_STRICT_AET_COMPARISON=true
# ORTHANC_OVERWRITE_INSTANCES=false
# Imaging web requiere compose/keycloak.yml + compose/imaging-auth.yml.
# IMAGING_OAUTH_COOKIE_SECRET=
# Dedicated Redis session password: openssl rand -hex 24 (48 lowercase hex).
# IMAGING_REDIS_PASSWORD=
# IMAGING_OAUTH_COOKIE_SECURE=false

# Status (requiere cargar compose/status.yml)
# GATUS_PORT=8086

# HTTPS (requiere cargar compose/ssl.yml)
# SSL_MODE=dev
# CERT_WEB_DOMAINS=localhost,127.0.0.1
# CERT_WEB_DOMAIN_COMMON_NAME=
# CERT_CONTACT_EMAIL=
# SSL_STAGING=false
# CERT_PROFILE=
# GATEWAY_HTTPS_PORT=443
# CERT_RSA_KEY_SIZE=2048
# CERT_TEMP_CERT_DAYS=365
# CERT_NGINX_STARTUP_WAIT=10
# CERT_RENEWAL_INTERVAL=12h
# CERT_ORG=Centro de Salud
# CERT_OU=sihsalus
# CERT_COUNTRY=PE
# CERT_STATE=Loreto
# CERT_LOCALITY=Maynas

# Seed restore (requiere cargar compose/seed.yml)
# SIHSALUS_SEED_URL=
# SIHSALUS_SEED_SHA256=
# SIHSALUS_SEED_PASSPHRASE_FILE=/secure/path/seed-passphrase
# SIHSALUS_SEED_FORCE=false
